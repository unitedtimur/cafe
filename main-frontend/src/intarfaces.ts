export interface IState {
    isBeginPage: boolean,
    volume: number,
    isMusicPlay: boolean,
}

export interface IIngredient {
    id?: string,
    src?: string,
    width?: number,
    height?: number,
    smallWidth?: number,
    smallHeight?: number,
}
